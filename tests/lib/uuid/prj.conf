CONFIG_ZTEST=y

CONFIG_UUID=y
CONFIG_UUID_V5=y

# Necessary configuration for the UUID library
CONFIG_BASE64=y
CONFIG_ENTROPY_GENERATOR=y

# Necessary extra configuration for the UUID v5 library
CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_MD=y
CONFIG_MBEDTLS_SHA1=y
